{% extends 'base.html.twig' %}

{% block title %}CourseType{% endblock %}

{% block body %}
    <h1>CourseType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ course_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('course_type_index') }}">back to list</a>

    <a href="{{ path('course_type_edit', {'id': course_type.id}) }}">edit</a>

    {{ include('course_type/_delete_form.html.twig') }}
{% endblock %}
